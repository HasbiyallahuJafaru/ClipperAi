# Phase 0 — Decisions

Verified 2026-09-14. Only non-obvious items were researched; the rest are well-known defaults.

| Component | Project | License | Integration | Decision |
|---|---|---|---|---|
| Acquisition | yt-dlp | Unlicense | Python library | **Use.** Datacenter IPs (Railway) often hit YouTube's bot wall — upload path must be first-class; residential proxy / cookies if URL path fails in prod. **YouTube now needs a JS runtime** (deno or node) on the machine — include one in the deploy image. |
| Captions font | Montserrat ExtraBold (`fonts/`, vendored) | SIL OFL 1.1 | `ass` filter `fontsdir` | **Used.** Same look on Windows and Linux. |
| Existing captions | yt-dlp `writeautomaticsub` (json3) | Unlicense | same library | **Later.** Auto-captions lack punctuation (hurts sentence boundaries), uploaded captions lack word timing, and Groq costs $0.04/hr. Add when STT spend is measurable. No extra dep needed (skip youtube-transcript-api). |
| STT | Groq `whisper-large-v3-turbo` | API | OpenAI-compatible HTTP | **Used.** $0.04/audio-hr, word timestamps via `verbose_json` + `timestamp_granularities=[word,segment]`. 610 s chunks (10 s overlap, stitched at seams) of 16 kHz mono **16-bit** FLAC, each encoded standalone (segment-muxer FLAC pieces → Groq 500). **Capacity:** free plan = 20 req/min, 7,200 audio-s/hour, 28,800 audio-s/day across all customers → upgrade to the Developer plan before launch. Batch API (50% off, 24 h+ window) possible for a slow/cheap tier. |
| STT fallback | onnx-asr + NVIDIA Parakeet TDT 0.6B v3 (int8, CPU) | MIT + CC-BY-4.0 | pip + 670 MB model | **Later.** ~$0.01/audio-hr on Railway CPU vs Groq $0.04, but 25 European languages only. Add when STT spend > ~$50/month or for Groq outages. faster-whisper on rented CPU costs more than Groq. xAI STT dropped — no key. |
| LLM | DeepSeek `deepseek-flash` (pass 1), `deepseek-v4-pro` (pass 2) | API | `openai` SDK, `base_url=https://api.deepseek.com` | **Use.** $0.15/$0.60 per 1M in/out (off-peak). JSON mode: `response_format={"type":"json_object"}` + word "json" in prompt; can return empty → retry. |
| SDK | openai-python | Apache-2.0 | pip | **Use** for both Groq and DeepSeek. One dep, no per-provider SDKs. |
| Validation | pydantic | MIT | pip | **Use.** Comes with FastAPI anyway. |
| Video processing | FFmpeg | LGPL/GPL (build-dependent) | CLI subprocess, arg arrays | **Use.** Separate process → no linking obligations. |
| Smart crop | YuNet face detector (`models/face_detection_yunet_2026may.onnx`, 230 KB, vendored) via OpenCV `FaceDetectorYN` + own shot logic | MIT (opencv_zoo) | vendored file | **Used (Phase 2).** Chosen over MediaPipe: OpenCV is needed for frame reads anyway, so one dep instead of two. **Avoid anything built on Ultralytics YOLO — AGPL-3.0.** Google AutoFlip is legacy. |
| CV | OpenCV (`opencv-python-headless` 5.0) | Apache-2.0 | pip | **Used** for frame reads + face detection. |
| Captions | ASS written by hand + FFmpeg `subtitles` filter (libass, ISC) | — | stdlib string formatting | **Use.** Word highlighting = ASS `\k` / per-word events. No caption engine dep. |
| Storage | Cloudflare R2 via boto3 (`storage.py`) | Apache-2.0 | pip | **Used (Phase 4).** S3 = same code, different endpoint. Clips/captions/thumbnails/clips.json → `projects/<id>/`, served as 24 h signed GET links. Uploads: signed PUT (content type signed; R2 has no POST-form uploads, so size is checked with HEAD before a project is accepted, 5 GB cap) → `uploads/<id>`. **Lifecycle via bucket rules, not app code:** `uploads/` 1 day, `projects/` 30 days (`python storage.py setup`, needs a token with Workers R2 Storage Write). Worker deletes its local scratch after every run and the uploaded source once the project is final. R2: $0.015/GB-month, free egress, 10 GB free. |
| S3 test double | moto (`ThreadedMotoServer`) | Apache-2.0 | pip, tests only | **Used** in test_jobs.py (rejects region `auto` on CreateBucket only). |
| Publishing | Buffer GraphQL API (`https://api.buffer.com`, Bearer) | API | stdlib `urllib` (`publishing.py`) | **Used (Phase 6, 2026-09-15).** Schema read by introspection and every request shape checked against the live API (unknown channel → "Channel not found", nothing posted). **Auth:** the workspace's personal API key (`BUFFER_API_KEY` in `.env`, like every other secret); Buffer's docs describe OAuth + PKCE but third-party app registration is reported closed, and one workspace needs no per-user tokens yet. Per-user keys/OAuth come with accounts (Phase 9). **Media (tested live):** Buffer reads the video at createPost *and* at send time, and **can't read R2 signed links** ("Video could not be read from its URL."; signed GET links also answer HEAD with 403). Chosen with the user: a second R2 bucket with public access (`clipperai-published`, `r2.dev` address for now, custom domain before launch); each post gets a server-side `CopyObject` to `<publication id>.mp4`, deleted once sent/failed/unscheduled, 45-day lifecycle backstop. Downloads are free (R2 egress), nothing streams through our servers, the main bucket stays private, scheduling goes up to 30 days. Rejected: making the main bucket public (exposes uploads), streaming through the API (server bandwidth cost, needs a public URL). Per network: YouTube (Shorts: vertical, ≥360 px) needs `metadata.youtube.title` **and `categoryId`** ("22" People & Blogs), Instagram `type` + `shouldShareToFeed`, Facebook `type` (reel); `needsApproval` is required. First real post 2026-09-15: private test Short to "The Micro-Fix", `shareNow` returned `sent` with `externalLink` within 2 s; then through the website: post now, scheduled (sent on time) and unscheduled posts. **Personal Instagram profiles** (channel `type: profile`) can't be posted to automatically ("require notification scheduling"), so they're shown as unusable; creator/business accounts are needed. Limits: 100 requests / 15 min per client → status checked only once a post is due, at most once a minute. Errors: HTTP 401 `UNAUTHENTICATED`, 429 with `Retry-After`, typed `MutationError` messages. Tests use `fake_buffer.py` (own code, same shapes). |
| Publishing alt | Ayrshare (paid SaaS) / Postiz (AGPL-3.0) | — | — | **Fallback only.** Postiz AGPL → only as unmodified separate service. |
| Job queue | Postgres `SELECT … FOR UPDATE SKIP LOCKED` on the `projects` table | PostgreSQL | SQL (`jobs.py`) | **Used (Phase 3).** No Redis, no queue lib. Heartbeat thread + 2-min stale requeue, 3 attempts with 2/4-min backoff, permanent errors fail fast, cancel checked at each step. |
| API | FastAPI + uvicorn | MIT + BSD-3 | pip (`api.py`) | **Used (Phase 3).** Shared bearer `API_KEY` until user accounts exist. |
| DB driver | psycopg 3 (`psycopg[binary]`) | LGPL-3.0 | pip, unmodified library | **Used.** LGPL is fine for an unmodified dependency in a SaaS. Migrations = plain `migrations/*.sql` + 15-line runner (no Alembic/ORM). |
| Dev database | pgserver (bundled Postgres 16) | Apache-2.0 | pip, dev/tests only | **Used locally** when `DATABASE_URL` is blank; Railway Postgres in production. |
| Website | Next.js 16 (App Router, Turbopack) + React 19 + Tailwind CSS 4 + TypeScript 5 (`create-next-app`) | MIT (Next, React, Tailwind) + Apache-2.0 (TypeScript) | npm, `apps/website` | **Used (Phase 5, 2026-09-14; redesigned 2026-09-15).** No UI kit, animation or data-fetching library (CSS keyframes, `fetch` + a 20-line polling hook). The browser calls the site's own `/api/*` route handler, which forwards to FastAPI with the Clerk session token. Route groups: `app/(site)` marketing pages (home, how-it-works, features, pricing, faq, sign-in/up) and `app/(app)` signed-in pages with a sidebar; URLs unchanged. Plan prices are copied in `app/plans.ts` (user's choice over a public backend route) and `walkthrough.mjs` fails if they drift from `billing.py`. |
| Website type | Geist Sans variable (`app/fonts/Geist-Variable.woff2`, vendored from the `geist` npm package 1.7.2) | SIL OFL 1.1 (`app/fonts/Geist-OFL.txt`) | `next/font/local` | **Used (redesign, 2026-09-15).** Neutral grotesk matching the reference design; a local file so builds never download fonts. Montserrat ExtraBold stays for clip captions shown on the site. |
| Heading accent type | Instrument Serif Italic (`app/fonts/InstrumentSerif-Italic.woff2`, latin subset from Google Fonts) | SIL OFL 1.1 (`app/fonts/InstrumentSerif-OFL.txt`) | `next/font/local`, `h1 em, h2 em` in `globals.css` | **Used (2026-09-15, user picked it from four options for a more stylish hero).** Key words of marketing headings in blue italic serif; everything else stays Geist. |
| Icons | `@phosphor-icons/react` 2.1 | MIT | npm | **Used (redesign, 2026-09-15).** One icon family incl. the six network logos; `/ssr` import in server components. |
| Sky photo | "Blue sky with white clouds" by uriel, Unsplash photo `xtgONQzGgOE` (`app/(site)/sky.jpg`, 2400×1800, re-encoded q80) | Unsplash License (free commercial use, no attribution required; no people, no endorsement issue) | `next/image` static import | **Used (redesign, 2026-09-15).** The reference design's sky; served resized as AVIF/WebP by Next. Sample clips on the site use placeholder gradients (user chose no generated people until a video we own exists). |
| Billing | Own code: `billing.py` plans dict + `subscriptions` table (one active row, DB partial unique index) + usage from existing tables | — | stdlib + SQL | **Used (2026-09-14), payments switched off by the user's decision:** pricing/checkout/billing pages show real prices (Creator $15, Pro $39, Business $99 a month) but subscribing activates instantly with `charged_cents = 0`; no payment provider, no card form. Limits (videos, hours of video, clips per calendar month) are enforced in the backend for every channel. One workspace until accounts exist. When payments go live: pick a provider with hosted checkout + webhooks (no card data on our servers), start its checkout in `billing.subscribe`, activate from the webhook, and use its billing period instead of calendar months. |
| Content package | `zipfile` (stdlib) streaming stored entries from R2 through FastAPI `StreamingResponse` | — | stdlib | **Used (2026-09-14).** Built on request, so edits/rejections are always reflected and nothing extra is stored. Ceiling: bytes pass through the API server; move to a worker-built ZIP in R2 behind a signed link if bandwidth matters. |
| Accounts | Clerk (hosted auth) + `@clerk/nextjs` 7.9 (website) + `clerk-backend-api` 7.0 (backend) + Clerk CLI 3.3 (setup) | SaaS; SDKs MIT | npm / pip | **Used (2026-09-15, user decision: Clerk for everything it offers around accounts).** Sign-in/sign-up UI, user database, sessions, Organizations for teams, OAuth for MCP clients; we store only Clerk ids as owners. Website: `ClerkProvider`, `proxy.ts` (`clerkMiddleware`, public `/`, `/how-it-works`, `/features`, `/pricing`, `/faq`, sign-in/up; the rest `auth.protect()`), the `/api` proxy forwards the session token. Backend: `authenticate_request(accepts_token=["session_token"], authorized_parties)` on every route; owner = `org_id` or `sub`. Cost: free up to 50,000 monthly retained users; Pro $25/mo ($20 annual) removes branding, adds MFA/passkeys; $0.02 per user beyond 50k. Development instance only so far. |
| Next.js patch | `next` 16.3.2 → **16.3.5** | MIT | npm | **Security fix (2026-09-15):** npm audit flagged critical RCE advisories for 16.0.0–16.3.2 (Windows-hosted servers; Image Optimization with AVIF). |
| MCP | official `mcp` Python SDK, Streamable HTTP | MIT | pip, mounted in FastAPI | **Use (Phase 8).** Auth through Clerk OAuth (verify `oauth_token` with `clerk-backend-api`); FastMCP also ships a Clerk provider. |
| Calendar | Own code: `publishing.plan` / `schedule` / `send_queued` + a day-by-day list page | — | stdlib + SQL | **Used (Phase 7, 2026-09-15).** Weekdays × times from a start date in the viewer's IANA time zone, one approved clip per free time up to 30 days ahead; posts are queued in `publications` (`status = 'queued'`) and handed to Buffer by a worker thread, so a month of posts never holds a request open and Buffer's request limit is waited out. No calendar component, no scheduler library. Time zones in SQL were tried first, but pgserver's Windows Postgres ships without its time zone files. |
| Time zones | `zoneinfo` (stdlib) + `tzdata` | Apache-2.0 | pip | **Used (Phase 7).** zoneinfo reads the OS zone database, which Windows and slim Linux images don't have; `tzdata` (maintained by CPython core developers) is its official fallback. Already installed as a psycopg dependency on Windows; listed in `requirements.txt` for Linux. |

## Monthly-ish unit cost (2 h podcast)
- Transcription: 2 h × $0.04 = **$0.08** (or $0 with existing captions)
- LLM: ~30k-token transcript, pass 1 flash + pass 2 pro on candidates ≈ **< $0.05**
- Dominant cost will be compute (FFmpeg render) + egress-free R2 storage, not AI.
