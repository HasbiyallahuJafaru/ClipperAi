include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - build/**
    - third_party/**  # copied Clerk SDK (pubspec.yaml dependency_overrides)
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**
