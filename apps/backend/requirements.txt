yt-dlp  # unpinned on purpose: YouTube changes often and only the latest release keeps up
curl_cffi==0.16.3  # yt-dlp needs it to impersonate a browser TLS fingerprint; Dailymotion refuses without it
openai==3.13.0
pydantic==2.13.5
opencv-python-headless==5.0.0.93
fastapi==0.141.1
uvicorn==0.53.0
psycopg[pool,binary]==3.3.5  # [pool] = psycopg_pool: shared connections instead of one per query
boto3==1.43.93
clerk-backend-api==7.0.0  # verifies Clerk session tokens (accounts)
redis==6.4.0  # rate limiting across instances when Railway provides REDIS_URL; without it the limiter stays in memory
tzdata==2026.4  # time zones for the content calendar where the OS has none (Windows, slim Linux images)
# local dev/tests only: pgserver (Postgres without installing it), moto[server] (fake S3 for test_jobs.py)
